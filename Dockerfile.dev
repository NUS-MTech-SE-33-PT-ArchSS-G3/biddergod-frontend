# Development Dockerfile for BidderGod Frontend
# Enables hot module replacement (HMR) for live development

FROM node:18-alpine

# Set working directory
WORKDIR /app

# Install dependencies first (for better caching)
COPY package.json package-lock.json* ./
RUN npm install

# Copy the rest of the application
COPY . .

# Expose Vite dev server port
EXPOSE 5173

# Start Vite dev server with host flag to allow external connections
# The --host flag is crucial for Docker to make the dev server accessible
CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0"]